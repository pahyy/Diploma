## Zagon

```bash
pip install -r requirements.txt

# grafični vmesnik (Streamlit chat)
streamlit run app.py

# terminalski način
python main.py
```

V `.env` morajo biti nastavljeni `OPENAI_API_KEY`, `LLM_MODEL` in
`SAFETY_IDENTIFIER`. Podatki: `data/retail_data_v2.csv`

